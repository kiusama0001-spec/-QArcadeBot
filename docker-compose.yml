services:
  web:
    build: .
    env_file: .env
    ports: ["8000:8000"]
    volumes: ["./data:/app/data"]
  bot:
    build: .
    env_file: .env
    command: python run_bot.py
    volumes: ["./data:/app/data"]
